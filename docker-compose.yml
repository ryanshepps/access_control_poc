version: '3.8'

services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: canary_poc_repo
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "6543:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: